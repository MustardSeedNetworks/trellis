name: License Compliance

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Run weekly on Mondays at 9am UTC
    - cron: '0 9 * * 1'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # The gate is defined once in the fleet-shared repo; this repo only wires it.
  # To change license policy for the fleet, edit that workflow, not this file.
  # foundation + modernc.org/mathutil are already ignored there fleet-wide.
  license-check:
    uses: MustardSeedNetworks/.github/.github/workflows/license-check.yml@4ffdaf668446e6ed6d4460d3267ef16ebde218d5 # v1.12.0
