name: License Compliance

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Run weekly on Mondays at 9am UTC
    - cron: '0 9 * * 1'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # The gate is defined once in the fleet-shared repo; this repo only wires it.
  # To change license policy for the fleet, edit that workflow, not this file.
  # foundation + modernc.org/mathutil are already ignored there fleet-wide.
  license-check:
    uses: MustardSeedNetworks/.github/.github/workflows/license-check.yml@048d173dd72ca0f9e5fa832fb5b9fd2a6447e5e9 # v1.13.0
