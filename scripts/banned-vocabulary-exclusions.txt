# Exact, reviewable exceptions for text that is not current product copy.
# Format: git path pattern | why the immutable or developer-only text is exempt
#
# Deliberately minimal. This file is NOT a copy of another repo's exclusions —
# stem's exempts protocol-standard identifiers and dated audit snapshots that do
# not exist here. Add an entry only when a real violation is found AND the text
# is genuinely immutable or developer-only.
#
# Reasons below deliberately avoid quoting the flagged terms, so this file does
# not trip the checker it configures.

# Immutable history and legal source text.
CHANGELOG.md | release-please generated immutable release history
LICENSE | unmodified BUSL-1.1 legal source text

# The policy source itself, which necessarily contains every flagged term.
scripts/i18n/banned-vocab.txt | policy term source

# Generated lockfile: hits are upstream package names, not product copy, and
# the file is rewritten by npm.
ui/package-lock.json | generated lockfile; hits are upstream package names

# Developer-facing comments that cite the agent-instructions file by name. That
# file is dev tooling and explicitly exempt; these are code comments, never
# customer-facing copy.
.gitignore | comment cites the agent-instructions file, developer tooling
ui/vite.config.ts | comment cites the agent-instructions file for the build contract

# Competitor comparison table: the flagged word describes a rival's cost model,
# not an MSN tier. The locked tier matrix has no such tier and this claims none.
docs/00-VISION.md | competitor cost-model comparison, not an MSN tier
